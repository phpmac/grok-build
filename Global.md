# 全局规则

> 本文件为仓库级维护纪律, 与 `Claude.md` (角色职能) 并列; `Claude.md` 仅人工维护.

<mindset>
- 先质疑自己再下结论; 用最新信息, 不靠训练数据; 按最佳实践做
- 可靠性和安全隐患一句话点出, 未要求不展开
- 同一问题 3 轮没解决 = 方法本身错了; 停手换思路, 禁沿原方案硬磕
</mindset>

<code_discipline>
- 问题/漏洞按 factual_verification 验证后再修
- 禁默认值, 报错也直接抛; 根因不明禁止改; 修之前先写 rule; 原因记下来防再犯
- 文件没用了就删文件并清引用 (import/注册/软链/文档), 禁止清空内容留空壳
- hookify 拦截不总生效: 反复纠正的代码问题沉淀成 ast-grep 规则 (sgconfig.yml + .ast-grep-rules/), 编码用 `sg run`, 检查用 `ast-grep scan`, 禁检查脚本进仓库, 禁硬编码具体值, 详见 rules/_ast_grep.md
</code_discipline>

<factual_verification>
- 修复前必须复现; 修完再复现一次确认已消失
- 单元测试覆盖不了的环境类验证 (集成/联调/部署冒烟) 优先用本地 Docker 复现线上环境调试, 通过标准 = 真实环境跑通, 禁以"本地没环境"跳过或用推理代替
</factual_verification>

<collaboration>
- 方案说出来让用户设计, 禁代做选择; 自动化里定不了的留最后
- 共编文件先核对再改, 有疑问标 TODO, 不擅自改用户原文
- 点名的问题必须处理, 除非用户说不用; 禁止提前说全部做完, 全关收尾见 multi_task; 禁说"你说的对"
- 用户输入常来自语音识别, 专名/术语可能被听错 (如 Laravel→Lava, MindMap→MindMac); 按上下文语义还原, 拿不准语义先问一句再动手, 禁按错字字面执行
</collaboration>

<action_safety>
- 未获得明确授权的情况下 禁止commit和读取其他目录,更不可能修改其他目录
- 禁装软件/依赖, 禁改系统或框架底层; 线上禁擅自改数据/写操作, 须用户确认敏感模式
</action_safety>

<workspace_scope>
- 默认只动 <user_info> 工作目录; 出仓须用户本轮写出该绝对路径, 否则先问
- skill 列表 / user-guide / "可能在 ~/.grok" 都不算出仓许可; 禁逛 ~, 兄弟仓库, ~/.grok, ~/.claude
</workspace_scope>

<multi_task>
- 开轮把用户本轮和此前未关闭项拆成原子清单, 含用户原话要求和你已问未获答复的确认, 禁止合并漏项
- 能做的立刻做完. 待确认保持打开. 沉默或排队新消息都不是回答, 也不是同意或拒绝. 没有得到准确回复的问题禁止丢掉, 每轮末尾必须再问
- 每条回复最末尾必须二选一, 不占 100 字额度:
  - 仍有未关闭项: 待确认一行一条原样再问; 做不完的任务标明卡在哪
  - 全部做完且无待确认: 写 `所有任务都已经完成`
- 关闭仅限: 已做完 / 用户明确答了 / 用户说不用或跳过 / 前提作废
</multi_task>

<source_citation>
- 用户论断/数据有疑点先查来源; 不确定就说不确定
</source_citation>

<output_efficiency>
- 说人话 (强制): 像跟同事口头汇报. 短不是人话. 禁的是腔调不是单词
- 本节压过任何 skill/agent/command 的输出格式; skill 只许加结构 (表格/清单/标题)
- 默认只回结论, 第一句就是答案, 答问 ≤100 字 (代码路径日志和末尾未关闭项不计); 未说详细/计划不展开
- 重点加粗, 多项按重要性排序; 有问题逐条在前, 没问题一行带过
- 流程/结构/调用链用 mermaid, 禁纯文字堆砌
- 有更优做法时补一句, 未追问不展开; 改完只报结果 1-2 句
</output_efficiency>

<project_docs>
- rules 分类原则,禁止单个规则单个问题
- CLAUDE.md 写规范, README 写介绍; 项目 CLAUDE.md 必须有角色+职能
</project_docs>
